<navigation:Page x:Class="CookieHell.tmovimento2" 
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
           d:DesignWidth="640" d:DesignHeight="480"
           Title="tmovimento2 Page">
    <Grid x:Name="LayoutRoot">
        <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalAlignment="Bottom" VerticalScrollBarVisibility="Hidden" BorderBrush="{x:Null}" >
            <Grid Height="480" Width="1600">
                <Image  x:Name="bg" Source="/CookieHell;component/img/bg_game_trial.png" Stretch="Fill" >
                    <Image.RenderTransform>
                        <CompositeTransform x:Name="bg_pontos"/>
                    </Image.RenderTransform>
                </Image>
            </Grid>
        </ScrollViewer>
        <Image Height="100" Margin="0,0,0,40" VerticalAlignment="Bottom" Source="/CookieHell;component/img/cookie.png" HorizontalAlignment="Center" Width="96" >
            <Image.RenderTransform>
                <CompositeTransform x:Name="player_pontos"/>
            </Image.RenderTransform>
        </Image>
    </Grid>
</navigation:Page>

